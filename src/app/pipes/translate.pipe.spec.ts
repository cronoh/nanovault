import { TranslateTPipe } from './translate.pipe';

describe('TranslateTPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateTPipe();
    expect(pipe).toBeTruthy();
  });
});
